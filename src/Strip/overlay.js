const CLASS_NAMES = {
  overlay: {
    base: 'stripe-overlay',
    afterOpen: '--after-open',
    beforeClose: '--before-close'
  },
  content: {
    base: 'stripe-content',
    afterOpen: '--after-open',
    beforeClose: '--before-close'
  }
}
